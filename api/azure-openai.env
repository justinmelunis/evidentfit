# Azure Key Vault Configuration
AZURE_KEY_VAULT_URL=https://kv-evidentfit.vault.azure.net/

# Azure Authentication (for local development)
# Option 1: Use Azure CLI (run 'az login')
# Option 2: Use Service Principal (uncomment and fill in)
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_TENANT_ID=your-tenant-id

# Azure AI Foundry Configuration (for local development)
# These will be used if Key Vault is unavailable
# FOUNDATION_ENDPOINT - Now retrieved from Key Vault (foundation-endpoint secret)
# FOUNDATION_KEY - Now retrieved from Key Vault (foundation-key secret)
FOUNDATION_CHAT_MODEL=gpt-4o-mini
FOUNDATION_EMBED_MODEL=text-embedding-3-small

# Azure AI Search Configuration (for local development)
# These will be used if Key Vault is unavailable
# SEARCH_ENDPOINT - Now retrieved from Key Vault (search-endpoint secret)
# SEARCH_QUERY_KEY - Now retrieved from Key Vault (search-query-key secret)
SEARCH_INDEX=evidentfit-index
INDEX_VERSION=v1-2025-09-25

# Key Vault Secret Names (update these to match your Key Vault secrets)
# FoundryApiKey - Your existing secret in Key Vault

# API Configuration
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002
DEMO_USER=demo
DEMO_PW=demo123
